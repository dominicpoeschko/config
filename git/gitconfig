[user]
    name = dominic
    email = mail@dominicp.de
[credential]
    helper = store
[core]
    pager = delta
    editor = nvim
[push]
    default = simple
[pull]
    rebase = false
[color]
	ui = auto
[interactive]
    diffFilter = delta --color-only
[delta]
    features = decorations
    navigate = true
    light = false
    line-numbers = true
    side-by-side = true
[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default
[mergetool "nvimdiff"]
  layout = "LOCAL,BASE,REMOTE / MERGED"
[submodule]
	fetchJobs = 8
[fetch]
	parallel = 8
[alias]
	reset = echo \"Use git stash instead of reset --hard. If you really need reset, use git-reset\"
	git-reset = \\!git
